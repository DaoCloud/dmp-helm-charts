dmp:
  tenant:
    code: "test" # !!! 注意要更改
    envoyLoglevel: "INFO"
    swOapHost: "dmp-management-dmp-skywalking-oap.testdmp.svc"
    swOapPort: "11800"
    twiceHost: "dmp-management-dmp-twice.testdmp.svc"
    twicePort: "7000"
    fateServer: "ws://dmp-management-dmp-fate.testdmp.svc:9056"
    esHost: "elasticsearch-master.testdmp.svc.cluster.local"
    esPort: "9200"
    adminIP: "0.0.0.0"
    gateway:
      replicas: 1
      service:
        type: NodePort
      image:
        envoy:
          repository: registry.dx.io/dx-pilot/dmp-envoy
          tag: develop-7074d4b  # Must be set explicitly
          pullPolicy: IfNotPresent
        fluentd:
          repository: registry.dx.io/dx-pilot/dmp-fluentd
          tag: develop-f488ed2  # Must be set explicitly
          pullPolicy: IfNotPresent
        thor:
          repository: registry.dx.io/dx-pilot/dmp-thor
          tag: develop-04211f9  # Must be set explicitly
          pullPolicy: IfNotPresent

############
imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""
